<h1> AJAX Puppy HQ  </h1>

<table>
  <th> Name </th>
  <th> Breed </th>
  <th> Adopt Me! </th>
  
  <% @puppies.each do |puppy| %>
    <tr>
      <td> <%= puppy.name %> </td>
      <td> <%= puppy.breed.name %> </td>
      <td> 
        <%= link_to 'Adopt', puppy, method: :delete, data: { confirm: 'Are you sure?' } %> 
      </td>
    </tr>
  <% end %>
</table>

<%= link_to 'Register New Puppy', new_puppy_path %>
