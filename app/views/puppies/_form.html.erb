<%= form_for(@puppy) do |f| %>
  <% if @puppy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@puppy.errors.count, "error") %> prohibited this puppy from being saved:</h2>

      <ul>
      <% @puppy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <br>
  
  <%= f.label :breed %>
  <%= f.select :breed_id, options_for_select(breed_array) %>

  <br>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
